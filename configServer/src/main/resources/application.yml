spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Munish034/config-server
          clone-on-start: true
server:
  port: 8085


